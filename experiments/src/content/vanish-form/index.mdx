---
name: "vanish-form"
title: "Vanish Form"
description: "Animated input with rotating placeholders and a vanish effect."
category: "UI"
version: "2.0.0"
dependencies: ["framer-motion", "react"]
source: "src/content/vanish-form/vanish-form.tsx"
---

# Vanish Form

Animated input with rotating placeholders and a vanish effect.

## Demo

<ComponentPreview name="vanish-form" />

## Usage

<DemoCode name="vanish-form" title="Demo Implementation" />

## Source Code

<ComponentCode name="vanish-form" title="Full Component" />

### Slow, Dramatic Animation

```typescript
// In animate() function, modify these values:
const sweepSpeed = 0.1;           // Very slow sweep
const disperseStrength = 0.1;     // Gentle dispersion
particle.life -= 0.008;           // Long fade
particle.vx *= 0.98;              // Extended drift
```

### Fast, Snappy Animation

```typescript
// In animate() function, modify these values:
const sweepSpeed = 0.5;           // Fast sweep
const disperseStrength = 0.3;     // Aggressive dispersion
particle.life -= 0.02;            // Quick fade
particle.vx *= 0.92;              // Rapid slowdown
```

## Dependencies

`framer-motion`

If you'd like additional sections (API, customization, or troubleshooting), tell me which parts to restore.

